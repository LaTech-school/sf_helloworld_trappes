<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>

        <header>
            <nav>
                <a href="{{ url('homepage') }}">Accueil</a> - 
                <a href="{{ url('legal') }}">Mentions Légale</a> - 
                <a href="{{ url('cookies') }}">Les Cookies</a> - 
                <a href="{{ path('cgu') }}">CGU</a> - 
            </nav>
        </header>

        {% block body %}Contenu par defaut{% endblock %}
    </body>
</html>
